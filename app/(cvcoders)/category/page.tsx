import React from 'react';
import CategoryPage from './_components/CategoryPage';

export const metadata = {
  title: 'CVCODERS : Category'
};

const page = () => {
  return (
    <div>
      <CategoryPage />
    </div>
  );
};

export default page;
